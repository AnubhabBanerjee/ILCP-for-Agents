# Pin loosely so downstream environments can resolve compatible wheels.
# The harness targets HuggingFace Qwen2.5-7B-Instruct (series alignment with Q4_K_M GGUF in llama.cpp; see README).
torch>=2.2.0
transformers>=4.43.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
datasets>=2.20.0
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.8.0
tqdm>=4.66.0
pytest>=8.0.0
